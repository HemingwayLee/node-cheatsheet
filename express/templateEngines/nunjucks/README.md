# How to run
```
npm init
npm install express
node index.js
```

Open browser and access `http://localhost:3000`

# Run with docker
```
docker build -t myexp .
docker run -it --rm -p3000:3000 myexp
```

